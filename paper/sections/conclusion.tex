\section{Conclusion}

We introduced ECBIT, an ERA5-conditioned transformer framework for sparse Antarctic AWS block imputation. The current implementation establishes the data pipeline, model family, baselines, and experiment matrix needed to test whether conditional ERA5 fusion improves long-gap reconstruction without degrading observed-variable representations. The final paper will complete the empirical analysis once remote GPU experiments are available.
