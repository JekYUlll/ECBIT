\section{Experiments}

\subsection{Data}
We scan 267 AntAWS 3-hourly station files and retain stations with record length of at least 10 years, temperature completeness of at least 70\%, mean core-variable completeness of at least 50\%, and minimum core-variable completeness of at least 20\%. This yields 27 main stations and five held-out stations: Butcher Ridge, Mount Sidley, Nico, Sabrina, and Zhongshan. ERA5 point time series are aligned to the exact station timestamps and normalized with statistics fitted on main-station train windows.

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{figures/fig_missing_patterns.pdf}
  \caption{Observed missingness in selected AntAWS stations. Wind speed and relative humidity show the largest missing burden, and high-percentile gaps last multiple days, motivating block-wise rather than point-wise imputation.}
  \label{fig:missing-patterns}
\end{figure}

\subsection{Baselines and Ablations}
Round 1 compares six baselines: linear interpolation, last-observation-carried-forward, ERA5 direct substitution after train-set bias correction, BRITS, SAITS, and an iTransformer imputation baseline. Round 2 evaluates ECBIT variants: full conditional cross-attention, no cross-attention, no ERA5, and no block-mask training. Round 3 evaluates held-out station generalization for ECBIT full at a 40\% missing rate.

\begin{figure*}[t]
  \centering
  \includegraphics[width=0.92\textwidth]{figures/fig_round1_baselines.pdf}
  \caption{Round 1 core baseline results over three block-missing regimes and three missing rates. iTransformer is the strongest completed baseline overall. Among stateless methods, ERA5 direct substitution is most competitive on long gaps, while linear interpolation is strongest on short gaps.}
  \label{fig:round1-baselines}
\end{figure*}

\input{tables/tab_round1_baselines}

\begin{figure*}[t]
  \centering
  \includegraphics[width=0.92\textwidth]{figures/fig_round2_ablations.pdf}
  \caption{Round 2 ECBIT ablation results over block-missing and MCAR masking regimes. Removing ERA5 substantially degrades block-imputation accuracy, while the current cross-attention implementation is statistically tied with the no-cross fusion variant. The MCAR training setting is easier and is therefore reported separately rather than compared directly to block-missing results.}
  \label{fig:round2-ablations}
\end{figure*}

\input{tables/tab_round2_ablations}

\subsection{Implementation Status}
The data pipeline, model implementations, training/evaluation entrypoints, and 315 YAML experiment configurations have been implemented. Round 1 core baselines are complete for interpolation, last-observation-carried-forward, ERA5 direct substitution, and iTransformer. Round 2 ECBIT ablations are complete. SAITS and BRITS are scheduled as heavier PyPOTS baselines after the core neural ablations finish. Completed Round 1 results show a consistent trade-off: ERA5 direct substitution is competitive for long block gaps, while linear interpolation is strongest among stateless methods for short gaps. Completed Round 2 results support the value of ERA5 conditioning: removing ERA5 increases block-missing MAE from 0.255 to 0.343. However, the full cross-attention model is tied with the no-cross fusion ablation, indicating that the present evidence supports ERA5-conditioned imputation more strongly than the specific cross-attention mechanism.
